##Baseball 
cd "C:\Users\markk\OneDrive\Desktop\CODE\baseball-dashboard"
npm run dev
python push_to_baseball.py

Python mlb/run_backtest_f5.py - Historical backtest
python mlb/generate_backtest_report.py - backtest report
python mlb/consensus_f5.py - MLB F5 Consensus Picks
python mlb/live_f5.py --interval 30 - Start the Live Polling Daemon:
python mlb/consensus_f5_v1_weather.py - To run V1 + Weather (V1.5):
python mlb/consensus_f5_v1.py - To run V1 (No Weather):
python mlb/consensus_f5_v2.py - To run V2 (Hybrid: Top-Down + V1 MC + Weather):

python mlb/live_cache.py - Run the daily morning cache manually (optional, the daemon auto-builds if missing):
python mlb/daily_f5_park_factors.py - Update Dynamic F5 Park Factors (must run before consensus)
python mlb/consensus_f5_v3.py - To run V3 (Tuned: Top-Down + Tuned Current MC + Weather):
python mlb/consensus_f5_v4.py - To run V4 (Tuned: Top-Down + Tuned Current MC + Weather + V5 Mathematical F5 Gatekeeper):
python mlb/post_process_fg.py --file .consensus_f5_v3_report_MLB_YYYY-MM-DD.json - BULLPEN POST-PROCESSING MODULE
python mlb/umpire_engine.py - Every Monday morning (before the daily run) 
python mlb/grade_td_reports.py - grader script for MLB (Gatekeeper picks only)

python grade_intl_reports.py --league KBO - grader script for KBO
python grade_intl_reports.py --league NPB - grader script for NPB
python kbo/consensus_f5_kbo.py - KBO F5 Morning Picks
python npb/consensus_f5_npb.py - NPB F5 Morning Picks




##Basketball
TRACKING_EPOCH = "2026-03-26"
python audit_team_names.py - check team names
/ **python grade_basketball.py  
python aggregate_basketball_stats.py*** 
python run_daily_sdi_report.py

# CLI Custom Audit
python run_custom_audit.py (-l or --limit flag)

# 🖥️ Interactive Dashboard for Custom Audit:
python -m streamlit run audit_dashboard.py

python audit_volatility_tiers.py



# 1. Update the raw stats from Proballers 
python scrape_proballers.py --file configs/proballers_leagues.txt --cutoff_date 2026-03-01 
python scrape_proballers.py --daily today --cutoff_date 2026-02-02 
python scrape_nbl1.py - For NBL 1 games only 
  
  1b. # Update the raw stats from api python auto_update_historical.py
  # 1c. Update the SDI index python calculate_sdi.py
    1d. python calibrate_from_local.py -  ← rebuild pace/eff pivots (run once every 2 weeks)

# 2. Convert raw stats into the True Offense/Defense Matrix
python generate_advanced_metrics.py - rebuild SRS matrix

# 3. Finally, run today's predictions using those updated matrices
python run_basketball_daily.py --mode full --date 2026-04-19 --refresh
python run_basketball_daily.py --date 2026-03-19 --no_match_center
python run_basketball_daily.py --mode full --no_match_center --date 2026-03-21

python push_to_dashboard.py --sport basketball

# Daily Confidence Report:
      # All fixtures today
        python run_confidence_report.py
        
      # Skip playoffs:
        python run_confidence_report.py --no_playoffs
      # Specific date
        python run_confidence_report.py --date 2026-05-06

      # Only show high-confidence games
        python run_confidence_report.py --min_score 60

      # Show component breakdown (V/M/B/S pts)
      python run_confidence_report.py --date 2026-05-06 --components

      # Filter by tier or league
        python run_confidence_report.py --tier top_domestic
        python run_confidence_report.py --league_id 72 

      # Only show games where BOTH teams have at least 7 graded games:
        python run_confidence_report.py --min_graded 7

        Pro Tip: If you run the script with the --components flag (python run_confidence_report.py --min_graded 7 --components), 
        it will print out the exact points the game scored in Volatility, MAE, Bias, and Spread. 
        This makes it instantly obvious if a game is being penalized for Bias (which you can adjust for) vs Volatility (which you should run away from).
        
       Just use:  python run_confidence_report.py --hidden_gems

      # confidence report audit 
         python standard_confidence_audit.py 


### Tennis Daily Workflow

```powershell
# Step 1 — Refresh in-season data (always re-downloads current year)
python setup_tennis_data.py

# Step 2 — Run ATP predictions
python tennis/consensus_tennis.py --tour ATP --iterations 10000

# Step 3 — Run WTA predictions
python tennis/consensus_tennis.py --tour WTA --iterations 10000

# Step 4 — Both tours in one pass
python tennis/consensus_tennis.py --tour BOTH

# Step 5 — Grade yesterday's predictions
python tennis/grade_tennis_reports.py


















# Discontinued workflows -- All workflow from here is not needed anymore 

#4 Update match centre 
cd football-dashboard
node scripts/sync-data.js
________________________________________________________________________________________________________________
________________________________________________________________________________________________________________
________________________________________________________________________________________________________________
Audit Summary Script -- backend only --Explanation of the metrics at the end of this page 
# Python audit_basketball.py

**Single day:**
python audit_basketball.py --date 2026-03-21

**Weekend audit (Fri → Sun):**
python audit_basketball.py --from 2026-03-19 --to 2026-03-21

**Filter by league:**
python audit_basketball.py --from 2026-03-19 --to 2026-03-21 --league "NBA"

**Show only busts, sorted by delta:**
python audit_basketball.py --from 2026-03-19 --to 2026-03-21 --tier BUST --sort delta

**Just wins, no game log (summary only):**
python audit_basketball.py --from 2026-03-19 --to 2026-03-21 --result win --no-log


________________________________________________________________________________________________________________
________________________________________________________________________________________________________________
________________________________________________________________________________________________________________

Calibration is handled by 

calibrate_league.py NOW python calibrate_from_local.py after run python compute_bias_corrections.py



 and can happen in two contexts:

1. Automatic (During Prediction Runs)
In 

run_basketball_daily.py
, when processing a league without an existing config, the script auto-calibrates on the fly:

run_basketball_daily.py → detects no configs/leagues/{id}.json
                        → calls calibrate_league.auto_calibrate(league_id)
                        → fetches finished games from API
                        → derives pace_pivot, eff_pivot, hca, regression_factor
                        → writes configs/leagues/{id}.json
                        → continues with prediction
You can skip this with --no_calibrate to only use existing configs.

2. Manual (Maintenance)
Run directly to recalibrate one or all leagues:

bash
python calibrate_league.py --league_id 40      # Recalibrate BBL Germany
python calibrate_league.py --all                # Recalibrate all leagues
What Calibration Actually Does
Fetches all finished games this season for the league from the API
Computes avg_total (mean game total) and std_total (variance)
Derives HCA from mean home margin, clamped to 0.5–8.0
Uses the tier's pace as pace_pivot (e.g., 78 for top_domestic)
Back-derives eff_pivot = (avg_total × 100) / (2 × pace_pivot) — this is the Fix 9 formula that ensures dimensional consistency with the engine
Sets regression_factor based on sample size (more games → higher trust, capped at 0.96)
Writes the config to configs/leagues/{id}.json
When Should You Recalibrate?
Start of season — When new leagues begin and have 15+ games
If MAE drifts — Run python backtest_v4.py and if accuracy drops, recalibrate
After league breaks — Mid-season breaks can shift team dynamics
Never during playoffs — Small sample + different play style would corrupt calibration
The calibration is essentially answering: "What does a typical game in this league look like?" — and the engine then adjusts from that baseline based on each specific matchup.




________________________________________________________________________________________________________________
________________________________________________________________________________________________________________
________________________________________________________________________________________________________________

#D1
Step 1: Copy the Data
Go to: Barttorvik Conference Only (https://barttorvik.com/trank.php?year=2026&conflimit=1)
Verify the "Conf. Games Only" filter is active.
Select the entire table (Click in table, Ctrl+A might work, or drag to select headers + rows).
convert to .csv with grok.
save to data/barttorvik.csv
Run: python ncaa/import_clipboard.py #to convert to json

Step 2: 
Run: python ncaa/fetch_barttorvik_conf.py #optional 

# Check if a team's conference stats differ from full-season
python verify_conf_data.py



3. Update NCAA Data
This refreshes team rankings, stats, and injury reports.

bash
# Fetch BartTorvik rankings (required for engine mapping)
python -m ncaa.fetch_barttorvik (https://barttorvik.com/trank.php?year=2026&json=1) TO barttorvik_raw.json
# Fetch NCAA team stats and standings
python -m ncaa.data_fetcher
# Fetch latest NCAA injury reports
python ncaa/fetch_injuries.py

4. Update NBA Data
This refreshes the schedule and advanced team/player metrics.

bash
# Fetch team and player statistics
python -m nba.fetch_nba_stats
python -m nba.fetch_nba_player_stats
# Fetch NBA injury reports
python -m nba.fetch_nba_injuries
# Fetch today's matchups
python -m nba.fetch_nba_schedule 

Step 5. Generate Predictions
bash
# Safe mode
python ncaa/predict_d1_conf.py --mode safe
# With injury data - Full Mode
python ncaa/predict_d1_conf.py --mode full
# Without injury data - Full Mode
python ncaa/predict_d1_conf_no_injuries.py 


# python core/supabase_pusher.py

# Show calculation details
python ncaa/predict_d1_conf.py --mode safe --trace

#Hybrid 
python ncaa/predict_d1_hybrid.py --date 2026-02-17 --mode safe

 Default 70/30 split
python ncaa/predict_d1_hybrid.py --date 2026-03-02

 Custom weights
python ncaa/predict_d1_hybrid.py --conf-weight 0.80 --full-weight 0.20 ----mode full

Compare Results:
bash
python compare_models.py --division d1 --mode full

# Grade only PLAY/LEAN decisions
python ncaa/audit_performance.py

# Include PASS decisions in the audit
python ncaa/audit_performance.py --include-pass

# Grade no inuries 
python ncaa/audit_performance.py --no-injuries


# Audit hybrid predictions
python ncaa/audit_performance.py --d1-hybrid

# With date override
python ncaa/audit_performance.py --d1-hybrid --date 2026-02-17

# Include PASS decisions
python ncaa/audit_performance.py --d1-hybrid --include-pass

##########


#To manually run the data pipeline and update the platform, you can execute the following steps in order from your terminal. I've designed these to follow the exact sequence used by the automated GitHub Action.



1. Update NCAA Data
This refreshes team rankings, stats, and injury reports.

bash
# Fetch BartTorvik rankings (required for engine mapping)
python -m ncaa.fetch_barttorvik (https://barttorvik.com/trank.php?year=2026&json=1) TO barttorvik_raw.json
# Fetch NCAA team stats and standings
python -m ncaa.data_fetcher
# Fetch latest NCAA injury reports
python ncaa/fetch_injuries.py

2. Update NBA Data
This refreshes the schedule and advanced team/player metrics.

bash
# Fetch team and player statistics
python -m nba.fetch_nba_stats
python -m nba.fetch_nba_player_stats
# Fetch NBA injury reports
python -m nba.fetch_nba_injuries
# Fetch today's matchups
python -m nba.fetch_nba_schedule 
python core/universal_bridge.py --league nba --mode safe  (for player props) 

3. Generate Predictions & Sync to Dashboard
Once the data is fresh, run the engine to calculate model totals and push the results to your Supabase backend.

bash
# Push all league predictions (NBA, NCAA, Euro, etc.) to the live dashboard
python core/supabase_pusher.py --league nba
# Grade yesterday's games
   python core/audit_engine.py --days 1 --league ncaa /nba 
# Not needed, as the supabase pusher run the predicition script


D2 
# Full D2 pipeline (fetch + predict)
python ncaa/run_d2.py

# Or run individually:
python ncaa/data_fetcher.py --division d2
python ncaa/predict_simple.py --division d2

# Advanced Model for D2 -- Run after using the d2 data fetcher 
python ncaa/predict_d2_hybrid.py


Run the NBA prediction engine (Safe Mode)
python -m nba.v1_2.run --mode safe

[!TIP] Why no --refresh? You might notice I didn't include the --refresh flag for ncaa.v1_2.run. Following my latest optimization, the scripts are now modular. It is much faster to run the fetchers individually (Steps 1 & 2) before running the engine (Step 3) to avoid "double-fetching" the same data.

do I need to push to Github for manual update of the pipeline? or is supabase pusher enough 

Thought for <1s


For a manual data update, 
supabase_pusher.py
 is enough. You do not need to push to GitHub to see the data update on your live platform.

Here is why:

1. The Data Flow
When you run the scripts locally:

Fetchers download data to your local 
data/
 folder.
Engine processes that local data to create predictions.
supabase_pusher.py
 takes those predictions and sends them directly to your Supabase Database.
Since your live website (the frontend) pulls its data directly from Supabase, the changes will appear instantly once the pusher finishes.

2. When do you need to push to GitHub?
You only need to push to GitHub in these two scenarios:

Code Changes: If you (or I) modify the Python logic, the API mapping, or the Frontend components, you must push to GitHub so the live site (on Render/Vercel) gets the new code.
Auto-Sync: If you want the cloud to handle the data updates automatically every 2 hours without you having to run anything on your computer.
Summary: If you just want to refresh the odds or scores right now, run the manual steps I provided earlier. Once 
supabase_pusher.py
 says "Push Complete," your website is updated! 🚀


Prediction Platform: All Systems Go 🏀🚀
I have streamlined the data pipeline and restored the V1.3 Universal Prediction Engine as the primary driver for both the dashboard and manual runs.

1. The Universal Engine (V1.3)
The system is now fully unified. The dashboard is powered by the logic in 
run_universal.py
 and core.universal_bridge.

Manual Running Instructions
To run predictions locally for any league, use the following commands:

NCAA (College Basketball):

python run_universal.py --league ncaa --mode safe
NBA (Pro Basketball):

python run_universal.py --league nba --mode safe
TIP

Use --mode full to include injury adjustments if you have updated injury notes in 
data/
.

2. NBA API Robustness 🛡️
The NBA data sync has been significantly strengthened to handle the frequent timeouts from stats.nba.com.

Robust Client: Centralized logic with 3x retries and exponential backoff.
ESPN Fallback: If the official API is down, the system automatically scrapes ESPN Team Stats (Offensive/Defensive data) to ensure predictions never stop.
Verification of Fallback
In our latest sync test, the official API timed out after 45s. The system successfully pivoted:

NBA API failed: Connection timed out...
Attempting ESPN fallback for NBA stats via scraping...
ESPN Fallback successful for 30 teams.
Successfully saved stats for 30 NBA teams.
3. Advanced Metrics Accuracy (NCAA)
I resolved the issue where eFG%, Turnover%, and Rebounding% were falling back to default values (50/18/28).

Validation Logic: The scraper now detects and rejects "default" data from centralized sources.
High-Quality Fallback: If the live CSV is blocked, the system now pulls from a high-quality raw data cache that preserves the true Four Factors for every team.
Result: Predictions for Kansas, Duke, and other teams now reflect their actual statistical profiles rather than league-wide averages.
4. GitHub Workflow Optimization ⚡
The 
.github/workflows/data_update.yml
 has been modularized for speed and reliability.

Modular Steps: Separate blocks for NCAA and NBA with 5-minute per-step timeouts.
Dual-Mode Generation: Now runs both --mode safe and --mode full for all leagues to ensure the dashboard has both standard and injury-adjusted data.
Redundancy Removed: Orchestration is now handled by the YAML file, following the V1.3 universal pattern.
Total Sync Time: ~7 minutes.
Workflow Logs
Review
Workflow Logs

Start Sync
Fetch NBA Stats/Injuries
Fetch NCAA Stats/Injuries
Universal Engine v1.3
Push to Supabase Store
Dashboard Updated
These changes ensure the system is both faster and more resilient than ever before.

5. Trust but Verify: Manual vs. Automated 🛠️
The GitHub Workflow is your "Workhorse"—it keeps the dashboard updated while you sleep. However, for maximum accuracy, you should adopt a Hybrid Approach:

When to run Manually?
High-Stakes Game Days: Run locally to see the full "Advanced Metrics" logs I've added to ensure teams aren't hitting defaults.
Injecting Manual Odds: If Vegas lines move significantly, place a ncaa_market_YYYY-MM-DD.csv in 
data/
 and run locally for immediate results.
Emergency Refresh: If the dashboard looks stale, run the fetchers manually to bypass temp blocks.
The "System Health" Tool
I created a diagnostic script for instant visibility:

python check_system_health.py
This will tell you exactly which files are stale, if teams are missing, or if metrics are currently falling back to defaults.

6. How to Inspect Failures
If the dashboard stops updating:

Go to your GitHub Repository -> Actions tab.
Click on the latest "NBA/NCAA Data Update" run.
Check the "Generate Predictions" step. The echo statements now clearly show success or failure for each league.


________________________________________________________________________________________________________________
________________________________________________________________________________________________________________
________________________________________________________________________________________________________________
## Glossary of Audit Metrics

### `Δ` — Absolute Delta *(per game, in the game log)*
> **"How far off was the model, regardless of direction?"**

```
Δ = |Actual Game Total − Model Projected Total|
```
Always a positive number. A Δ of `8.0` means the model was 8 points off — whether the game went higher or lower than projected. This is what drives the **Tier** (BULLSEYE/SOLID/BUST etc).

---

### `±Δ` — Signed Delta *(per game, in the game log)*
> **"Which way did the model miss?"**

```
±Δ = Actual Game Total − Model Projected Total
```
- **`+8.0` (green)** → game scored **8 more points** than the model expected *(game went OVER)*
- **`−8.0` (red)** → game scored **8 fewer points** than the model expected *(game went UNDER)*

Same magnitude as `Δ`, but tells you the direction of the error.

---

### `AVG Δ` — Average Absolute Delta *(league breakdown)*
> **"On average, how many points does the model miss by in this league?"**

The mean of all `Δ` values for a league. Lower is better. This is your **accuracy score** for the league — equivalent to MAPE but in raw points.

| AVG Δ | Meaning |
|---|---|
| ≤ 8.5 | 🟢 Very accurate |
| ≤ 14.5 | 🟡 Respectable |
| > 14.5 | 🔴 Struggling |

---

### `SIGNED Δ` — Average Signed Delta *(league breakdown)*
> **"Does the model consistently overshoot or undershoot this league?"**

The mean of all `±Δ` values for a league. This reveals **systematic bias**:

- **`+4.2` (red)** → model consistently underestimates scoring in this league — games tend to go **Over** the model's projection
- **`−4.2` (green)** → model consistently overestimates scoring — games tend to go **Under** the model's projection
- **Near 0** → no systematic bias, errors are random noise

---

### `BIAS` *(league breakdown)*
> **"Plain-English label for the signed delta direction"**

| BIAS | Signed Δ | Meaning |
|---|---|---|
| 📈 Over | < −3 | Model overshoots — back Unders in this league |
| 📉 Under | > +3 | Model undershoots — back Overs in this league |
| ✅ Neutral | −3 to +3 | No significant directional lean |

The threshold of ±3 points is deliberate — anything smaller is statistical noise for basketball games averaging 150+ points.

________________________________________________________________________________________________________________
________________________________________________________________________________________________________________
________________________________________________________________________________________________________________

## Football (Soccer) - World Cup 2026 Engine

# 1. Background Server (Dashboard)
python wc2026_server.py
* This runs a local web server at http://localhost:8000
* Serves the interactive WC2026 Dashboard with live Elo ratings, fixtures, and BTTS predictions.

# 2. Manual Results Feed (Live Elo Recalibration)
* The World Cup engine relies on manual entry of completed match results to recalibrate live Elo ratings (K=40).
* Open run_wc2026.py and navigate to the ACTUAL_RESULTS dictionary (around line 465).
* Add completed match results in the format:
  ("Home Team", "Away Team"): (Home Goals, Away Goals),
  Example: ("IR Iran", "New Zealand"): (2, 2),
* Save the file. The `wc2026_server.py` daemon will automatically detect the changes and update the dashboard.

# 3. Running Predictions Manually (CLI)
python run_wc2026.py --standings
* Runs the core engine and displays the full group standings and predictions in the terminal.

python run_wc2026.py --elo-report
* Displays the detailed Elo Report (Base -> Live -> Effective with Confederation Adjustments).

python run_wc2026.py --group A --trace
* Shows detailed calculation trace (xG, Elo deltas, probabilities) for a specific group.

---

## 🏆 FIFA World Cup 2026 — Group Stage (Offline Predictor)

**No API required.** All 72 group stage fixtures are embedded with Elo-based predictions.
Results update automatically from live standings — no refreshes needed.

```bash
# Full group stage — all predictions + standings + export
python run_wc2026.py --standings --export

# Single group
python run_wc2026.py --group A --standings

# All games for one team
python run_wc2026.py --team Spain

# Today's upcoming games only
python run_wc2026.py --upcoming

# Completed games with result check
python run_wc2026.py --completed

# BTTS value picks only
python run_wc2026.py --btts-only

# Matchday filter
python run_wc2026.py --matchday 2

# Sharp mode + math trace
python run_wc2026.py --group H --mode full --trace
```

### Updating Results Daily
After games finish, feed in scores using the helper — no manual file editing:

```bash
# Single result
python wc2026_update.py --result "Spain 2-0 Cabo Verde"

# Multiple results at once
python wc2026_update.py --result "Belgium 1-0 Egypt" --result "Saudi Arabia 0-1 Uruguay"

# Interactive mode (paste multiple scores)
python wc2026_update.py

# Check what's already logged
python wc2026_update.py --check

# See today's fixtures
python wc2026_update.py --today
```

Output file: `data/football/wc2026_predictions_YYYY-MM-DD.json`

#start server 
python wc2026_server.py


> [!NOTE]
> The WC2026 engine uses **Elo → xG → Poisson** math calibrated to international
> tournament baselines (BTTS ~44%, avg total ~2.45 goals). All 72 fixtures span
> Jun 11 – Jun 28. Matchday 3 games in each group are played simultaneously.

---