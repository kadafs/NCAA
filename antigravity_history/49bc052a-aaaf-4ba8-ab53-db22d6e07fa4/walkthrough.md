# Push to GitHub Walkthrough

I have successfully pushed the latest changes to the [NCAA repository](https://github.com/kadafs/NCAA).

## Changes Pushed

Successfully committed and pushed the following:

- **New Files**:
    - `ncaa/predict_d1_hybrid.py`: The new hybrid prediction model (70% conference / 30% full season).
    - `data/d1_hybrid_predictions.json`: Initial predictions generated by the hybrid model.
- **Updated Data**:
    - Various JSON and CSV data files in the `data/` directory (e.g., `barttorvik_stats.json`, `d1_conf_predictions.json`) reflecting the latest stats fetch and prediction runs.
- **Refinements**:
    - `utils/mapping.py`: Updates to team name mappings.
    - `ncaa/audit_performance.py`: Logic updates for performance auditing.

## Verification

The push was confirmed with the following output:
```text
To https://github.com/kadafs/NCAA.git
   c26bdbf..db5fcbc  master -> master
```
